import type {CapacitorConfig} from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "cl.divasbeautyspa.app",
    appName: "Divas Beauty Spa",
    webDir: "android-assets"
};

export default config;
